industrial_complex_equivalent = {
    color_type = good
    value_type = number
    precision = 0
    category = all
}
arms_factory_equivalent = {
    color_type = good
    value_type = number
    precision = 0
    category = all
}

# let it be known that i despise this - wobbly
# fucking paradox

trade_laws_cost_factor = {
    color_type = bad
    value_type = percentage
    precision = 0
    category = all
}
economy_cost_factor = {
    color_type = bad
    value_type = percentage
    precision = 0
    category = all
}
mobilization_laws_cost_factor = {
    color_type = bad
    value_type = percentage
    precision = 0
    category = all
}